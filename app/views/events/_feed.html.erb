<div class="column ">
    <% if user_signed_in? %>
        <article class="media box">
        <!--
            <figure class="media-left">
                <p class="image is-64x64">
                    
                </p>
            </figure>
        -->
            <div class="media-content">
                <%= render form_path, form_object1: form_object, object_desciption: :description, object_date: :event_date %>
            </div>
        </article>
    <% end %>


    <% form_objects.each do |post_feed| %>
        <%= render 'show_form_main', name_user: post_feed.creator.name, 
        username_user: post_feed.creator.username, title_form: post_feed.title, 
        date_form: post_feed.event_date, description_form: post_feed.description, 
        form_view: post_feed, edit_path_form: edit_event_path(post_feed), 
        id_user: post_feed.creator_id %>    
    <% end %>
</div>
